apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  labels:
    app: grafana
type: Opaque
data:
  # Default username: admin
  admin-username: YWRtaW4=  # base64 encoded "admin"
  # Generate a secure password and encode it with: echo -n "your-secure-password" | base64
  admin-password: YWRtaW4xMjM=  # base64 encoded "admin123" (change this in production)
