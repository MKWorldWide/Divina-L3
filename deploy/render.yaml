services:
  - type: web
    name: divina-l3
    env: node
    plan: free
    buildCommand: npm ci || npm install
    startCommand: npm start || node server.js || npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: LOG_LEVEL
        value: info
